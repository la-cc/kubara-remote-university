---
apiVersion: kubara.io/v1alpha1
kind: ServiceDefinition
metadata:
  name: vcluster-platform
  annotations:
    kubara.io/category: multi-tenancy
spec:
  chartPath: vcluster-platform
  status: enabled
  clusterTypes:
    - hub