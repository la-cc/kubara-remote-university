apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: spoke-2
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: spoke-2
    server: "https://kubernetes.default.svc"
  project: hub-dev
  sources:
    - repoURL: https://github.com/la-cc/kubara-remote-university.git
      targetRevision: main
      path: platform-components/helm/vcluster
      helm:
        ignoreMissingValueFiles: true
        releaseName: "spoke-2"
        valueFiles:
          - values.yaml
        values: |
          vcluster:
            controlPlane:
              proxy:
                extraSANs:
                  - spoke-2.hub-dev.stackit.run
              ingress:
                enabled: false

          ingressRouteTCP:
            enabled: true
            host: spoke-2.hub-dev.stackit.run
            annotations:
              external-dns.alpha.kubernetes.io/target: hub-dev.stackit.run
  ignoreDifferences:
    - group: apps
      kind: StatefulSet
      name: spoke-2
      namespace: spoke-2
      jsonPointers:
        - /spec/updateStrategy
        - /spec/volumeClaimTemplates
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=false
      - PruneLast=true
      - FailOnSharedResource=true
      - RespectIgnoreDifferences=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
