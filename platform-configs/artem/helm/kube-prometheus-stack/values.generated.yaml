# AUTO-GENERATED; DO NOT MODIFY;
# This file is auto-generated by kubara and should not be modified manually in any way.
# To customize values, you can add additional files named values-*.yaml to this directory.

kube-prometheus-stack:
  global:
    imagePullSecrets:
      - name: image-pull-secret
    imageRegistry: ""
  prometheusOperator:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
    prometheus:
      monitor:
        additionalLabels:
          monitoring.instance: artem-dev
  prometheus:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        traefik.ingress.kubernetes.io/router.middlewares: oauth2-proxy-oauth-auth@kubernetescrd
      enabled: true
      paths:
        - /prometheus
      pathType: ImplementationSpecific
      hosts:
        - artem-dev.stackit.run
      tls:
        - secretName: tls-prometheus
          hosts:
            - artem-dev.stackit.run


    prometheusSpec:
      retention: 10d
      serviceMonitorSelector:
        matchLabels:
          monitoring.instance: artem-dev
      storageSpec:
        volumeClaimTemplate:
          spec:
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 10Gi
      externalUrl: https://artem-dev.stackit.run/prometheus
      routePrefix: /prometheus
    thanosServiceMonitor:
      serviceMonitor:
        additionalLabels:
          monitoring.instance: artem-dev
  alertmanager:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        traefik.ingress.kubernetes.io/router.middlewares: oauth2-proxy-oauth-auth@kubernetescrd
      enabled: true
      paths:
        - /alertmanager
      pathType: ImplementationSpecific
      hosts:
        - artem-dev.stackit.run
      tls:
        - secretName: tls-alertmanager
          hosts:
            - artem-dev.stackit.run


# Below is an example alertmanager.config section:
# It demonstrates an advanced routing setup that includes Microsoft Teams (v2), a watchdog receiver
# (e.g. for UptimeRobot) and a silenced route for informational alerts.
# This config use webhook urls from secret-files (see alertmanagerSpec below)
#
#    config:
#      route:
#        receiver: msteams-notifications  # Default receiver if no route matches
#        routes:
#          # 1. Silence all alerts named "InfoInhibitor"
#          - receiver: "null"
#            matchers:
#              - alertname =~ "InfoInhibitor"
#          # 2. Route "Watchdog" alerts to UptimeRobot with a short repeat interval
#          - receiver: uptimerobot
#            repeat_interval: 1m
#            matchers:
#              - alertname =~ "Watchdog"
#          # 3. Route all alerts with severity "warning" or "critical" to Microsoft Teams
#          - receiver: 'msteams-notifications'
#            matchers:
#              - severity =~ "warning|critical"
#            continue: true  # Continue to evaluate other routes after this one
#      receivers:
#        # Microsoft Teams v2 receiver using `msteamsv2_configs`
#        - name: 'msteams-notifications'
#          msteamsv2_configs:
#            - send_resolved: true
#              webhook_url_file: /etc/alertmanager/secrets/msteams-webhook-url
#        # Watchdog/Uptime monitoring receiver
#        - name: uptimerobot
#          webhook_configs:
#            - url_file: /etc/alertmanager/secrets/uptimerobot-webhook-url
#        # Null receiver to discard matched alerts
#        - name: "null"
#      # Optional: Use custom templates for consistent alert formatting
#      templates:
#        - /etc/alertmanager/config/*.tmpl

    alertmanagerSpec:
#      # secrets for alertmanager config webhook urls
#      secrets:
#        - msteams-webhook-url
#        - uptimerobot-webhook-url

      externalUrl: https://artem-dev.stackit.run/alertmanager
      routePrefix: /alertmanager

  kube-state-metrics:
    prometheus:
      monitor:
        additionalLabels:
          monitoring.instance: artem-dev
  kubeApiServer:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  kubelet:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  kubeControllerManager:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  coreDns:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  kubeDns:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  kubeScheduler:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  thanosRuler:
    serviceMonitor:
      additionalLabels:
        monitoring.instance: artem-dev
  grafana:
    admin:
      existingSecret: grafana-admin-credentials
      userKey: admin-user
      passwordKey: admin-password
    serviceMonitor:
      labels:
        monitoring.instance: artem-dev
    ingress:
      ingressClassName: traefik
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
        traefik.ingress.kubernetes.io/router.middlewares: oauth2-proxy-oauth-auth@kubernetescrd
      enabled: true
      path: /grafana
      pathType: ImplementationSpecific
      hosts:
        - artem-dev.stackit.run
      tls:
        - secretName: tls-grafana
          hosts:
            - artem-dev.stackit.run
    envFromSecrets:
      - name: oauth2-credentials
    grafana.ini:
      server:
        root_url: https://artem-dev.stackit.run/grafana/
        serve_from_sub_path: true
      auth.github:
        name: "Github"
        enabled: true
        client_id: ${client-id}
        client_secret: ${client-secret}
        allow_sign_up: true
        auto_login: false
        scopes: "user:email,read:org"
        allowed_organizations: "[\"la-demos\"]"
        role_attribute_path: contains(groups[*], '@la-demos/g-gitops') && 'Admin' || 'Viewer'
    additionalDataSources:
      - access: proxy
        isDefault: false
        name: loki
        type: loki
        url: http://loki-headless.loki.svc.cluster.local:3100
        version: 1
prometheus-blackbox-exporter:
  image:
    pullSecrets:
      - image-pull-secret
  global:
    imageRegistry: quay.io

externalSecrets:

  secretStoreRef:
    kind: ClusterSecretStore
    name: artem-dev


  secrets:
    grafana-admin-credentials:
      dataFrom:
        - remoteKey: artem/dev/kube-prometheus-stack/grafana_credentials
    oauth2-credentials:
      dataFrom:
        - remoteKey: artem/dev/kube-prometheus-stack/grafana_oauth2_credentials

namespace:
  labels:
    project-name: "artem"
    stage: "dev"
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/audit: privileged
    pod-security.kubernetes.io/warn: privileged
